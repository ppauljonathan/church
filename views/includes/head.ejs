<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=title%></title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="title-bar">
        <div class="logo-box">
            <img src="/logo.png" alt="logo" class=" centered logo">
        </div>
        <div class="heading-box">
            <p class="centered heading">Sermons Control Panel</p>
            <div class="centered navbar">
                <a href="/" class="nav-el <%if(title=='Sermons'){%>selected<%}else{%><%}%>">Home</a>
            <a href="/upload" class="nav-el <%if(title=='Upload Sermon'){%>selected<%}else{%><%}%>">Upload</a>
            </div>
        </div>
    </div>
    <div class="body-container">
